# 表單欄位群組功能使用指南

## 概述

群組功能允許您將表單欄位分類組織，在表單渲染時會以群組標題顯示，讓表單結構更清晰易讀。

## 功能特性

### 1. 群組管理
- 新增群組
- 編輯群組名稱
- 刪除群組（會將欄位移到「未分組欄位」）

### 2. 欄位分組
- 將欄位分配到不同群組
- 在群組內調整欄位順序
- 支援多個群組

### 3. 視覺化顯示
- 在表單設計器中按群組分類顯示欄位
- 在表單渲染時顯示群組標題
- 群組標題使用醒目的樣式

## 使用方式

### 方式一：在欄位編輯時設定群組

1. 新增或編輯欄位
2. 在「群組名稱」欄位中：
   - 從下拉選單選擇現有群組
   - 或直接輸入新群組名稱
   - 或留空（不屬於任何群組）

### 方式二：使用群組管理功能

1. 點擊「管理群組」按鈕
2. 新增群組：
   - 輸入群組名稱
   - 點擊「儲存」
3. 編輯群組：
   - 點擊群組標題旁的編輯按鈕
   - 修改群組名稱
   - 點擊「儲存」
4. 刪除群組：
   - 在群組管理對話框中點擊「刪除群組」
   - 確認刪除

## 範例：物料申請表單的群組設定

### 基本資訊群組

將以下欄位設定為「基本資訊」群組：
- 申請ID (id)
- 料號 (item_code)
- 產品大類 (main_category_id)
- 產品中類 (sub_category_id)
- 產品小類 (spec_category_id)
- 中文名稱 (item_name_cn)
- 英文名稱 (item_name_en)

**設定步驟：**
1. 編輯每個欄位
2. 在「群組名稱」中輸入或選擇「基本資訊」
3. 設定適當的顯示順序

### 物料資訊群組

將以下欄位設定為「物料資訊」群組：
- 基本材質 (material)
- 表面處理 (surface_finish)
- 尺寸規格 (dimensions)

### 訂購資訊群組

將以下欄位設定為「訂購資訊」群組：
- 最小訂購量 (moq)
- 單位 (unit)
- 供應商 (supplier_id)

## 表單渲染效果

設定群組後，表單會按以下方式顯示：

```
┌─────────────────────────────────┐
│ 基本資訊                        │
├─────────────────────────────────┤
│ [申請ID] [料號]                │
│ [產品大類] [產品中類] [產品小類]│
│ [中文名稱] [英文名稱]           │
└─────────────────────────────────┘

┌─────────────────────────────────┐
│ 物料資訊                        │
├─────────────────────────────────┤
│ [基本材質] [表面處理]           │
│ [尺寸規格]                      │
└─────────────────────────────────┘

┌─────────────────────────────────┐
│ 訂購資訊                        │
├─────────────────────────────────┤
│ [最小訂購量] [單位] [供應商]    │
└─────────────────────────────────┘
```

## 群組管理功能詳解

### 新增群組

1. 點擊「管理群組」按鈕
2. 在對話框中輸入群組名稱
3. 點擊「儲存」

**建議的群組名稱：**
- 基本資訊
- 分類資訊
- 物料資訊
- 尺寸規格
- 訂購資訊
- 客戶資訊
- 供應商資訊
- 備註與說明
- 申請流程資訊
- 審核資訊
- 成本資訊
- 庫存資訊
- 其他資訊

### 編輯群組

1. 點擊群組標題旁的編輯圖示
2. 修改群組名稱
3. 點擊「儲存」

**注意：** 修改群組名稱會更新所有屬於該群組的欄位。

### 刪除群組

1. 在群組管理對話框中點擊「刪除群組」
2. 確認刪除

**注意：** 刪除群組不會刪除欄位，只是將欄位的群組設定清空，欄位會移到「未分組欄位」。

## 群組內欄位排序

在群組內，可以使用上下按鈕調整欄位順序：

1. 找到要調整順序的欄位
2. 點擊欄位左側的上/下按鈕
3. 欄位會在群組內移動
4. 顯示順序會自動更新

## 最佳實踐

### 1. 群組命名

- 使用清晰、描述性的名稱
- 保持命名一致性
- 建議使用中文名稱（因為是顯示給使用者看的）

### 2. 群組數量

- 建議每個表單使用 3-8 個群組
- 太多群組會讓表單看起來複雜
- 太少群組可能無法有效分類

### 3. 欄位分配

- 相關的欄位應該放在同一群組
- 每個群組建議包含 2-10 個欄位
- 避免群組內欄位過多

### 4. 顯示順序

- 重要的群組放在前面
- 相關的群組放在一起
- 使用邏輯順序（例如：基本資訊 → 詳細資訊 → 其他資訊）

## 多選項設定範例

### 範例 1：產品大類（下拉選單）

**欄位設定：**
- 欄位鍵值：`main_category`
- 欄位類型：`select`
- 欄位標籤：`產品大類`
- 群組名稱：`基本資訊`
- 必填：是

**選項設定：**
```
值：H  標籤：H - Handle (把手)
值：S  標籤：S - Slide (滑軌)
值：M  標籤：M - Module/Assy (模組)
值：D  標籤：D - Decorative Hardware (裝飾五金)
值：F  標籤：F - Functional Hardware (功能五金)
值：B  標籤：B - Builders Hardware (建築五金)
值：I  標籤：I - Industrial Parts Solution (工業零件)
值：O  標籤：O - Others (其他)
```

### 範例 2：標籤（多選下拉）

**欄位設定：**
- 欄位鍵值：`tags`
- 欄位類型：`multiselect`
- 欄位標籤：`標籤`
- 群組名稱：`其他資訊`
- 必填：否

**選項設定：**
```
值：urgent     標籤：緊急
值：important  標籤：重要
值：new        標籤：新產品
值：custom     標籤：客製化
值：standard   標籤：標準品
```

**使用說明：**
- 使用者可以選擇多個標籤
- 值會以陣列形式儲存：`["urgent", "important"]`
- 顯示為 chips 形式

### 範例 3：優先級（單選框）

**欄位設定：**
- 欄位鍵值：`priority`
- 欄位類型：`radio`
- 欄位標籤：`優先級`
- 群組名稱：`申請流程資訊`
- 必填：否

**選項設定：**
```
值：HIGH    標籤：高
值：MEDIUM  標籤：中
值：LOW     標籤：低
```

### 範例 4：包裝選項（複選框）

**欄位設定：**
- 欄位鍵值：`packaging_options`
- 欄位類型：`checkbox`
- 欄位標籤：`包裝選項`
- 群組名稱：`包裝說明`
- 必填：否

**選項設定：**
```
值：plastic_bag      標籤：塑膠袋
值：bubble_wrap      標籤：氣泡袋
值：color_box        標籤：彩盒包裝
值：recycle_symbol   標籤：回收標誌
值：product_label    標籤：產品標籤
值：manual           標籤：說明書
```

**使用說明：**
- 使用者可以勾選多個選項
- 值會以陣列形式儲存：`["plastic_bag", "color_box", "product_label"]`

## 測試步驟

### 1. 測試群組功能

1. 開啟表單設計器
2. 新增幾個欄位
3. 將欄位分配到不同群組
4. 切換到「預覽」標籤
5. 確認群組標題正確顯示
6. 確認欄位按群組分類

### 2. 測試群組管理

1. 點擊「管理群組」按鈕
2. 新增一個新群組
3. 將欄位移到新群組
4. 編輯群組名稱
5. 刪除群組（確認欄位移到未分組）

### 3. 測試多選項

1. 新增一個 `select` 類型的欄位
2. 點擊「載入範例」按鈕
3. 修改範例選項
4. 新增更多選項
5. 切換到「預覽」測試選項是否正確顯示

## 常見問題

### Q: 如何將欄位移到不同群組？

A: 編輯欄位，在「群組名稱」中選擇或輸入新的群組名稱。

### Q: 可以將欄位從一個群組移到另一個群組嗎？

A: 可以，編輯欄位並修改群組名稱即可。

### Q: 刪除群組會刪除欄位嗎？

A: 不會，刪除群組只會將欄位的群組設定清空，欄位會保留。

### Q: 群組名稱可以重複嗎？

A: 可以，但建議使用不同的群組名稱以便區分。

### Q: 多選項的值如何儲存？

A: multiselect 和 checkbox 會儲存為 JSON 陣列格式，例如：`["tag1", "tag2"]`

## 相關文件

- `FORM_DESIGNER_README.md` - 表單設計器完整說明
- `FIELD_OPTIONS_EXAMPLES.md` - 多選項設定範例
- `DYNAMIC_FORMS_README.md` - 動態表單系統說明
